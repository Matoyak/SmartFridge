namespace SmartFridge.Services {
    //function unique() {

    //    this.orderBy = this.$filter('orderBy');
    //    this.order = function (predicate) {
    //        this.predicate = predicate;
    //        this.reverse = (this.predicate === predicate) ? !this.reverse : false;
    //        this.testItems = this.orderBy(this.testItems, predicate, this.resverse);
    //    };
    //    this.order('expDate', true);

    //}
    //angular.module("SmartFridge").filter("unique", unique);
}
